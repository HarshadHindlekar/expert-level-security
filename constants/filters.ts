import { type ScanStatus, type ScanType } from '@/types/scan';

export const allStatuses: ScanStatus[] = ['Completed', 'Scheduled', 'Failed', 'Running'];
export const allTypes: ScanType[] = ['Greybox', 'Blackbox'];
